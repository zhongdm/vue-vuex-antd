import Login from '@/pages/Login.vue'
import Vue from 'vue'

describe('username and password', () => {
  it('corrent username', () => {
    // const Constructor = Vue.extend(Login)
    // const vm = new Constructor().$mount()
    // console.log('WELCOME', vm.$el.querySelector('#username'))
    // expect(vm.$el.querySelector('#username').value)
      // .toEqual('admin')
    expect(2+2).toBe(4)
  })
})
